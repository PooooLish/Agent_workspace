# External API

- Do not write real keys into tracked files.
- Prefer environment variables for real credentials.
- Use `env.example` only as a placeholder template.
- Local files such as `.env.local` should not be committed to git.
- Document verification steps without exposing secret values.
