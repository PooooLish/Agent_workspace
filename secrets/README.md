# Secrets

This folder stores templates only. Do not put real secrets here.

- Real API keys should live in system environment variables or a password manager.
- Do not ask an agent to print, store, or commit real secrets.
- Use `env.example` only as a placeholder reference.
