# Sandboxes

Use this directory for temporary experiments that are not formal tasks.

Recommended use:

- quick prototypes
- throwaway command tests
- environment experiments
- short investigations

Promote useful work into `tasks/<task_name>/` only when it becomes a formal task.
Remove sandbox experiments only with explicit approval when cleanup is needed.
