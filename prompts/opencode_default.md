# OpenCode Default Prompt

Please read `AGENTS.md`, `README.md`, and the current task's `task.md` before acting.

Requirements:

- Keep edits inside the current task directory.
- Do not delete files.
- Do not save real secrets.
- Share a short execution plan first.
- Verify with the minimum useful command after edits.
- Report changed files, commands run, and verification result at the end.
